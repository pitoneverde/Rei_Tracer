#include "minirt.h"
#include <math.h>

int main(int argc, char **argv)
{
    if (argc > 2)
        PRINT_ERR("Invalid argument count: %d", argc);
    (void)argv;
    // parse_input(argc, argv);
    // init();
    // launch();
    // cleanup();
    return (0);
}