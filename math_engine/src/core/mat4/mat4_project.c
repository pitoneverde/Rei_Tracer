// NOTE TO SELF: this is not really needed for miniRT, but it could be useful for a rasterization preview

// #include "core/mat4.h"

// // ======= Projection Matrices ========
// t_mat4	mat4_orthographic(float left, float right, float bottom, 
// 						float top, float near, float far);
// t_mat4	mat4_perspective(float fov, float aspect, float near, float far);
